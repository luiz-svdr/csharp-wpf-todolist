<Window x:Class="MyToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyToDoList"
        mc:Ignorable="d"
        Background="#001D39"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="My ToDo List" Height="950" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="800"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>     
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Source="{StaticResource Logo}"                
               Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4"/>
        <Button x:Name="AddTask" Click="BtnAddTask_Click"
                Style="{StaticResource AddTaskButtonStyle}"                
                Grid.Column="3" Grid.Row="2" Margin="-5,0,0,0"/>
        <ItemsControl x:Name="TaskListControl"
              Grid.Row="3" Grid.RowSpan="9" Grid.Column="3">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource TaskPanelBorderStyle}">
                            <TextBlock Text="{Binding Description}" Style="{StaticResource TaskPanelTextBlockStyle}"/>
                        </Border>

                        <Button Grid.Column="1" Style="{StaticResource DeleteButtonStyle}"
                        Click="DeleteTask_Click"
                        CommandParameter="{Binding}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </Grid>
</Window>
